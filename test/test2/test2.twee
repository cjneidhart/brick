:: StoryTitle
Test 2

:: StoryData
{
  "ifid": "D5585FCC-9F03-41AE-9D24-73BF341E66E6",
  "format": "Brick",
  "format-version": "0.0.1"
}

:: Start
This is the start passage.
It has some <code>Brick markup</code>.

Next: @linkTo('Passage 2')


:: Passage 2
<p>
  This is a second passage.
  This time, we use <code>\<p\></code> elements for paragraph breaking.
  Here's an example <code>if</code> statement:
</p>
<p>
  @if (Math !== undefined) {
    Math is real!
  } @else {
    Math is not real\?
  }
</p>
<p>@linkTo('Start', 'Return to Start')</p>
